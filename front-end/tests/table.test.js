import { describe, it, expect } from "vitest";
import { dateStringFormatCToDate } from "../js/table";

describe("dateStringFormatCToDate", () => {
  it("devolve Array<JSON> do ficheiro em texto enviado como argumento", () => {
    expect(dateStringFormatCToDate("38/01/2023")).toBe();
  });
});
